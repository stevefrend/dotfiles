.DS_Store
ansible/.ansible/
ansible/local.vars.yml

# Local/secret zsh env — never commit. The .example templates ARE tracked.
.zshenv
.zshenv.*
!.zshenv.example
!.zshenv.*.example

# Machine-local git identity/overrides — never commit. Template IS tracked.
.gitconfig.local
!.gitconfig.local.example
